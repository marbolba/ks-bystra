import { styled } from "@mui/material/styles";

const RadioButtonsWrapper = styled("div")`
  display: flex;
  flex-direction: column;
  gap: 40px;
`;

const OptionWrapper = styled("div")`
  display: flex;
  gap: 8px;
  align-items: center;
`;

export default { RadioButtonsWrapper, OptionWrapper };
