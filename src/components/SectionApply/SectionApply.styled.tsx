import { Paper } from "@mui/material";
import { styled } from "@mui/material/styles";

const StyledPaper = styled(Paper)`
  padding-block: 80px;
  padding-inline: 120px;
`;

export default { Paper: StyledPaper };
